# Relatórios de legado (marco 2.0)

Regra de corte aplicada no arquivamento:

- `ISSUE-*` com número `< 039` foram movidas para `../issues/`.
- `EXEC-*` com número `< 040` foram movidas para `../execucoes/`.
- A fila ativa foi reiniciada em `ISSUE-001` no arquivo `../../execution_queue.csv`.
