# Serialization Security

Documento resumido desta camada de segurança/resiliência.

- arquitetura em camadas opcional
- compatibilidade retroativa preservada
- limitações e trade-offs documentados

Consulte testes em `tests/serialization/` e benchmark em `benchmarks/serialization_security_benchmark.py`.
