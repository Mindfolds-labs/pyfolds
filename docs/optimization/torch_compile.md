# torch.compile Benchmark

Use `run_compile_benchmark()` para comparar eager vs compile.

Métricas: throughput, latência média, warmup.

Recomendação: habilitar `torch.compile` apenas após benchmark no hardware alvo.
