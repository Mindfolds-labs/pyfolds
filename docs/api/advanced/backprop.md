# API Advanced — Backpropagation

Backpropagação dendrítica para sinal soma→dendrito.

Parâmetros:
- `backprop_enabled` (habilita acoplamento funcional no caminho dendrítico)
- `backprop_delay`
- `backprop_signal`
- `backprop_amp_tau`
