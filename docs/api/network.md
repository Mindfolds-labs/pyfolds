# API — Network

## `MPJRDNetwork`

Container para composição de múltiplas unidades/camadas MPJRD com conectividade explícita.

Uso típico:

```python
from pyfolds import MPJRDNetwork
```

> Consulte também `api/layers.md` para integração por camadas.
