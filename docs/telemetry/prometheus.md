# Prometheus

Ative com `TelemetryConfig(enable_prometheus=True, prometheus_port=8000)`.

Métricas:
- `pyfolds_events_total` (Counter)
- `pyfolds_forward_latency_ms` (Histogram)
- `pyfolds_buffer_utilization` (Gauge)
