# Canonical runtime dependencies mirror [project.dependencies] in pyproject.toml
# Optional stacks (tensorflow, examples, serialization, telemetry) are exposed as extras.
torch>=2.0
numpy>=1.24
zstandard>=0.21
google-crc32c>=1.5
reedsolo>=1.7
